"use server";

/**
 * Upload Server Actions (Stub)
 * -----------------------------
 * Placeholder for file upload functionality.
 */

export async function getSignedUploadUrl(filename: string, filetype: string) {
  return {
    success: false,
    error: "Upload not configured",
    url: "",
  };
}
